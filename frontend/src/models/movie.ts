export interface Movie {
    id: string
    title: string
    description: string
    posterUrl: string
    backdropUrl: string
    rating: number
    year: number
    duration: string
    genres: string[]
    director: string
  }